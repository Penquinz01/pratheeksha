What i want:
- Authentication
- A section to fetch data from the backend
- A section to add new data to the backend
- A section to update existing data
- A section to delete existing data

I want it to be ligth weight
first work on the backend use a secure and fast backend no need to use something heavy 
Give me choices for backend
you are not allowed change the db or this file
create a new copy of the db file if needed
The web should be used in production use docker if needed 

Each file should only do its own job
it should be highlt modular
if i wanted to create a new feature i should be able to do it without changing existing code